name: my_account_book
description: "Premium Monthly Account Book"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.1
  intl: ^0.19.0
  shared_preferences: ^2.2.2
  fl_chart: ^0.66.0
  # 충돌 및 네트워크 403 Forbidden 에러를 방지하는 최적 버전
  excel: 2.1.0  
  path_provider: ^2.1.2
  share_plus: ^7.0.0

flutter:
  uses-material-design: true
